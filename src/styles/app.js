import { StyleSheet } from 'aphrodite/no-important'

const app = StyleSheet.create({
  container: {
    width: '1000px',
    margin: '0 auto',
    paddingTop: '30px',
    fontFamily: 'PT Sans'
  }
});

export default app
